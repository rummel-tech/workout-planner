name: health_integration
description: Health data integration with HealthKit/Google Fit
version: 0.0.1
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  home_dashboard_ui:
    path: ../home_dashboard_ui

flutter:
  plugin:
    platforms: {}
